import styled from "@emotion/styled";
import { Position } from "../Position";

import { PositionProps } from "styled-system";

export const Static = styled(Position)<PositionProps>({ position: "static" });
